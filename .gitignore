# 런타임/데이터 — 코드 아님
# 엑셀 원본은 제외. app/data.js는 정적 배포에 필요해서 커밋 대상(공개 저장소 주의).
*.xls
*.xlsx
*.db
*.db-shm
*.db-wal
*.db*.bak
uploads/
*.log

# 시크릿
.env*
*.key
*.pem
credentials*.json
service-account*.json

# 파이썬 캐시
__pycache__/
*.pyc
